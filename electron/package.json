{
  "name": "hz-cc-flow-electron",
  "private": true,
  "version": "1.0.0",
  "description": "HZ CC Flow - Claude Code GUI",
  "author": "HZ",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "pack": "electron-builder --config electron-builder.yml",
    "run:pack": "electron-builder --config electron-builder.yml",
    "dev": "tsc && electron dist/main.js"
  },
  "dependencies": {
    "electron-updater": "^6.3.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.6.0",
    "@types/node": "^22.0.0",
    "electron": "^32.0.0",
    "electron-builder": "^25.0.0",
    "typescript": "^5.7.0"
  }
}
